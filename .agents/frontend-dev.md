# Frontend development notes

- Stack: React + TypeScript + Tailwind CSS as requested.
- Local structure: `src/main.tsx`, `src/app/App.tsx`, `src/pages/HomePage.tsx`, `src/components/*`.
- Build: existing tooling (Vite) is expected; run `npm install` and `npm run dev` in the project root.
- Design system: tokens in `src/styles/globals.css` and utility classes for primary/secondary buttons.
- Next steps: integrate editorial content, images, accessibility review, and add tests.

- Nota: a pasta `Liberte-se Experience` presente no repositório é apenas um exemplo/apoio — o trabalho principal do MVP está na raiz `src/`.
